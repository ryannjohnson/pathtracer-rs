mod camera;
mod color;
mod constants;
mod hit;
mod material;
mod matrix;
mod random;
mod ray;
mod render;
mod scene;
mod vector;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
